version: '3.8'

services:
  web:
    image: clothing-rental-web:latest
    container_name: clothing-rental-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - PORT=8080
      - ConnectionStrings__DefaultConnection=Host=163.61.73.83;Port=5432;Database=ClothingRental;Username=postgres;Password=123123@
    volumes:
      - uploads-data:/app/wwwroot/uploads

volumes:
  uploads-data:
